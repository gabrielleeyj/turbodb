# Environment for turbodb-sync.service. Install as /etc/turbodb/sync.env
# (mode 0640, owner root:turbodb-sync) — it contains the database password.
TURBODB_PG_DSN=postgres://turbodb_sync:CHANGE_ME@127.0.0.1:5432/appdb
