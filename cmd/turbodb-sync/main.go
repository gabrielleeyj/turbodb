// TurboDB Sync is the CDC consumer that replicates data from PostgreSQL to the engine.
package main

import (
	"fmt"
	"os"
)

func main() {
	fmt.Println("turbodb-sync: not yet implemented")
	os.Exit(0)
}
